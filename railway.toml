
[build]
builder = "NIXPACKS"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/"
healthcheckTimeout = 100

[start]
cmd = "node src/index.js"

[variables]
RAILWAY_DISABLE_SLEEP = "true"
